<!--
osgEarth Sample

MetaCarta TMS Feed - Projected TMS

Uses the TMS driver to access a projected dataset.  Note the trailing "/" on the URL.
The TMS driver reads all the profile information from an XML config file on the server, so
usually all you need to do is specifiy a URL.
-->

<map name="TMS Example" type="flat">
    <image name="boston-freemap" driver="tms">
        <url>http://labs.metacarta.com/wms-c/Basic.py/1.0.0/boston-freemap/</url>
    </image>
  
</map>

INCLUDE_DIRECTORIES(${OSG_INCLUDE_DIRS} )
INCLUDE_DIRECTORIES(${S2_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${OPENSSL_INCLUDE_DIR})

SET(TARGET_LIBRARIES_VARS OSG_LIBRARY OSGDB_LIBRARY OSGUTIL_LIBRARY OSGVIEWER_LIBRARY OPENTHREADS_LIBRARY S2_LIBRARY)
link_libraries(OpenSSL::SSL OpenSSL::Crypto)

SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /J")
ADD_DEFINITIONS(-D_USE_MATH_DEFINES)
ADD_DEFINITIONS(-DNOMINMAX)

SET(TARGET_SRC osgearth_s2.cpp )

#### end var setup  ###
SETUP_APPLICATION(osgearth_s2)
/** CMake Template File - compiled into AutoGenShaders.cpp */
#include <osgEarthExtensions/bumpmap/BumpMapShaders>

#define MULTILINE(...) #__VA_ARGS__

using namespace osgEarth::BumpMap;

Shaders::Shaders()
{
    Vertex = "BumpMap.vert.glsl";
    _sources[Vertex] = MULTILINE(@BumpMap.vert.glsl@);

    Fragment = "BumpMap.frag.glsl";
    _sources[Fragment] = MULTILINE(@BumpMap.frag.glsl@);
};

#ifndef OSGEARTH_TILE_GENERATOR_H
#define OSGEARTH_TILE_GENERATOR_H 1

#include <osgEarth/Common>
#include <osgEarth/TileBuilder>
#include <osg/Node>

namespace osgEarth
{
    // note: package internal; do not export
    class GeocentricTileBuilder : public TileBuilder
    {
    public:
        GeocentricTileBuilder( MapConfig* map, const std::string& url_template );

        osg::Node* createNode( const PlateCarreCellKey& key );
        
    private:
        osg::Node* createQuadrant( const PlateCarreCellKey& key );

    //private:
    //    osg::ref_ptr<PlateCarreTileSource> image_source;
    //    osg::ref_ptr<PlateCarreTileSource> field_source;
    //    bool make_dem;
    };
}

#endif // OSGEARTH_TILE_GENERATOR_H
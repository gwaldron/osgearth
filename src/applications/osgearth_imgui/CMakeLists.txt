INCLUDE_DIRECTORIES(
    ${OSG_INCLUDE_DIRS}
    ${OSGEARTH_SOURCE_DIR}/src/imgui
    ${OSGEARTH_SOURCE_DIR}/src/imgui/examples
)
SET(TARGET_LIBRARIES_VARS OSG_LIBRARY OSGDB_LIBRARY OSGUTIL_LIBRARY OSGVIEWER_LIBRARY OPENTHREADS_LIBRARY)
IF(GLEW_FOUND)
    INCLUDE_DIRECTORIES(${GLEW_INCLUDE_DIR})
    LIST(APPEND TARGET_LIBRARIES_VARS GLEW_LIBRARY)
ENDIF(GLEW_FOUND)

SET(TARGET_SRC
    ${OSGEARTH_SOURCE_DIR}/src/imgui/imgui.cpp
    ${OSGEARTH_SOURCE_DIR}/src/imgui/imgui_demo.cpp
    ${OSGEARTH_SOURCE_DIR}/src/imgui/imgui_draw.cpp
    ${OSGEARTH_SOURCE_DIR}/src/imgui/imgui_widgets.cpp
    ${OSGEARTH_SOURCE_DIR}/src/imgui/examples/imgui_impl_opengl3.cpp
    ${OSGEARTH_SOURCE_DIR}/src/osgEarth/OsgImGuiHandler.cpp
    osgearth_imgui.cpp )

IF(TRACY_FOUND)
    INCLUDE_DIRECTORIES(${TRACY_INCLUDE_DIR})
    LIST(APPEND TARGET_LIBRARIES_VARS TRACY_LIBRARY)
ENDIF(TRACY_FOUND)

#### end var setup  ###
SETUP_APPLICATION(osgearth_imgui)
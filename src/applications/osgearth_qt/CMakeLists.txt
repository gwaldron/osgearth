INCLUDE( ${QT_USE_FILE} )

INCLUDE_DIRECTORIES(${OSG_INCLUDE_DIRS} ${QT_INCLUDES})
SET(TARGET_LIBRARIES_VARS osgEarthQt OSG_LIBRARY OSGDB_LIBRARY OSGMANIPULATOR_LIBRARY OSGUTIL_LIBRARY OSGVIEWER_LIBRARY OSGQT_LIBRARY OPENTHREADS_LIBRARY ${QT_ALL_LIBRARIES})

SET(MOC_HDRS
    DemoMainWindow
)

QT4_WRAP_CPP( MOC_SRCS ${MOC_HDRS} )

SET(TARGET_H
    DemoMainWindow
)

SET(TARGET_SRC
    ${MOC_SRCS}
    osgearth_qt.cpp
)

#### end var setup  ###
SETUP_APPLICATION(osgearth_qt)

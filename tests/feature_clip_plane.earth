<!--
osgEarth Sample
Use this earth file with the osgearth_clipplane example.

This demonstrates how to render geometry on the earth's surface
with depth testing off and a horizon clip plane. This is one
way to mitigate z-fighting issues.
-->

<map name="Clip plane demo">

    <GDALImage name="world">
        <url>../data/world.tif</url>
    </GDALImage>

    <FeatureModel name="boundaries">
        <OGRFeatures name="world">
            <url>../data/world.shp</url>
        </OGRFeatures>        
        <styles>
            <style type="text/css">
                states {
                   stroke:            #ffff00;
                   render-depth-test: false;
                   render-clip-plane: 0;
                   render-order:      1;
                }                    
            </style>
        </styles>        
    </FeatureModel>

</map>

<!--
  TileCache Example
  
  TileCache's "Disk"-style cache creates a folder hierarchy with
  cached image files. This driver lets you access that cache directly
  without going through a server application. You can either access the
  raw files locally, or grab them over HTTP. Handy for off-line datasets.
  Use tilecache_seed to prepopulate your TileCache cache.
-->

<map name="TileCache Demo" type="geographic">
   
   <image name="world" driver="tilecache">
       <url>http://pelicanmapping.com/data</url>
       <layer>world</layer>
       <format>jpeg</format>
   </image>
   
</map>

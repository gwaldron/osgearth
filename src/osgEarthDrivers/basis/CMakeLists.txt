IF(BASISU_FOUND)

SET(TARGET_SRC
    ReaderWriterBasis.cpp
)

INCLUDE_DIRECTORIES( ${BASISU_INCLUDE_DIR} )
SET(TARGET_LIBRARIES_VARS BASISU_TRANSCODER_LIBRARY )

#### end var setup  ###
SETUP_PLUGIN(basis)

ENDIF(BASISU_FOUND)
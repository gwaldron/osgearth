IF(WEBP_FOUND)

SET(TARGET_SRC
	ReaderWriterWebP.cpp
)

INCLUDE_DIRECTORIES( ${WEBP_INCLUDE_DIR} )

SET(TARGET_LIBRARIES_VARS WEBP_LIBRARY OPENTHREADS_LIBRARY OSG_LIBRARY OSGDB_LIBRARY)

SETUP_PLUGIN(webp)

ENDIF(WEBP_FOUND)